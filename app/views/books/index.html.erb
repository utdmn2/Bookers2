
  <% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this obj from being saved:
  <% @book.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>

<div class="container">
 <div class="row">
  <%= render 'users/sidebar', book: @book, user: @user  %>

   <div class="col-md-8 offset-md-1">

  <h1>Books</h1>
   <table class="table">
    <thead>
    <tr>
     <th scope="col"></th>
     <th scope="col">Title</th>
     <th scope="">Opinion</th>
    </tr>
    </thead>

   <tbody>

    <% @books.each do |book| %>
    <tr>
     <td><%= link_to user_path(book.user) do %>
      <%= attachment_image_tag book.user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %>
      <% end %></td>
     <td><p><%= link_to book.title,book_path(book.id) %></p></td>
     <td><p><%= book.body %></p></td>
  <!-- <# if @bookfav.favorited_by?(current_user) %>-->
  <!--  <td><%#= link_to book_favorites_path(@bookfav), method: :delete do %>-->
  <!--      ♥<%#= @bookfav.favorites.count %>-->
  <!--      <#% end %>-->
  <!--  </td>-->
  <!-- <#% else %>-->
  <!--  <td>-->
  <!--      <%#= link_to book_favorites_path(@bookfav), method: :post do %>-->
  <!--      ♡<%#= @bookfav.favorites.count %>-->
  <!--      <#% end %>-->
  <!--  </td>-->
  <!--<#% end %>-->
    </tr>
    <% end %>
   </tbody>
  </table>

   </div>
  </div>
</div>